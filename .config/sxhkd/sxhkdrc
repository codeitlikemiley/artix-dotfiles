# Spawn Terminal
super + Return
    st
# Password Manager
super + shift + space
    rofi-pass
# Drop Down Terminal
super + grave
#    tdrop -ma -w 100% -h 40% -s main st -c tdrop-st -t goldcoderst
   tdrop -ma -w 100% -h 40% -s main alacritty --class tdrop-st

# HJKL as arrow Key
hyper + h
    xdotool keyup h key --clearmodifiers Left

hyper + j
    xdotool keyup j key --clearmodifiers Down

hyper + k
    xdotool keyup k key --clearmodifiers Up

hyper + l
    xdotool keyup l key --clearmodifiers Right
#  Toggle Capslock
hyper + Return
   xdotool key Caps_Lock
